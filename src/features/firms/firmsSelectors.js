export const selectFirms = (state) => state.firms.content;
export const selectPagination = (state) => state.firms.pagination;
export const selectCurrentFirm = (state) => state.firms.currentFirm;
export const selectFirmsStatus = (state) => state.firms.status.firms;
export const selectFirmsError = (state) => state.firms.error;
export const selectFirmsMessage = (state) => state.firms.message;
